#include <bits/stdc++.h>
using namespace std;

class Node
{
public:
    int val;
    Node *left;
    Node *right;

    Node(int val)
    {
        this->val = val;
        this->lelft = NULL;
        this->right = NULL;
    }
};

int main()
{
    Node *root = new Node(10);
    Node *a = new Node(10);
    Node *b = new Node(10);
    Node *c = new Node(10);
    Node *d = new Node(10);
    Node *e = new Node(10);
    Node *f = new Node(10);
    Node *g = new Node(10);
    Node *h = new Node(10);
    Node *i = new Node(10);

    return 0;
}